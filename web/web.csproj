<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- The only reason this file exists is so Visual Studio does file nesting correctly -->
    <TargetFramework>netstandard2.1</TargetFramework>
    <RootNamespace>web</RootNamespace>
  </PropertyGroup>

</Project>
